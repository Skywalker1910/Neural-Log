# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv*/
.pytest_cache/
env/
ENV/
*.egg-info/
dist/
build/

# Database
*.db
*.db-wal
*.db-shm
# Database backups taken before a risky migration (R6 took one of these).
*.bak
*.db.*
*.sqlite
*.sqlite3

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Exports
exports/*.xlsx

# Environment
.env
.env.local
.env.*
!.env.example
*.pem
*.key
.claude/
.codex/
.aws/
deploy/known_hosts
backups/

# Local dev server runs (see the run-locally workflow)
app_run.log
app_run.pid
frontend/vite.log
frontend/vite.pid

# Frontend (Vite + React). dist/ is a build artifact - rebuilt via `npm run build`.
frontend/node_modules/
frontend/dist/

# Per-user data written at runtime - personal log content (wake times, workouts,
# notes) and the Path templates the app regenerates on demand. This repo is
# public; none of it belongs here. Source art for the icon set lives directly in
# artifacts/ and stays tracked.
artifacts/checklists/
artifacts/paths/
artifacts/checklist_items/
